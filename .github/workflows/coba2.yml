name: Android Release Build 9

on:
  workflow_dispatch:

defaults:
  run:
    working-directory: ./coba9

jobs:
  build:
    name: Build Release APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      # Setup Gradle dengan build-root-directory yang benar
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6
        with:
          # Arahkan root project ke subfolder coba9
          cache-encryption-key: ${{ secrets.GRADLE_CACHE_ENCRYPTION_KEY }}
          # Set false dulu jika ingin workflow_dispatch selalu memperbarui cache
          cache-read-only: false 
          add-job-summary: 'always'

      - name: Run R8 Configuration Analyzer
        run: |
          chmod +x ./gradlew
          ./gradlew :app:analyzeReleaseR8Config --no-daemon --stacktrace

      - name: Upload R8 Analyzer Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: r8-config-analyzer-report-${{ github.run_number }}
          path: |
            coba9/app/build/reports/r8/
            coba9/app/build/outputs/mapping/
          if-no-files-found: warn

      - name: Build APK (Optimized)
        run: |
          chmod +x ./gradlew

          # Hapus 'clean' agar build cache & configuration cache optimal
          ./gradlew :app:assembleRelease \
            --no-daemon \
            --non-interactive \
            --stacktrace \
            --console=plain
        env:
          NO_COLOR: "1"
          ORG_GRADLE_PROJECT_android_native_build_output_ignore_errors: true

      - name: Sign APK
        uses: r0adkll/sign-android-release@v1
        id: sign_app
        env:
          BUILD_TOOLS_VERSION: "34.0.0"
        with:
          releaseDirectory: coba9/app/build/outputs/apk/release
          signingKeyBase64: ${{ secrets.KEYSTORE_BASE64 }}
          alias: ${{ secrets.KEY_ALIAS }}
          keyStorePassword: ${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: pos-offline-release-signed-apk
          path: |
            ${{ steps.sign_app.outputs.signedReleaseFile }}
            coba9/**/*.json
          retention-days: 1
          if-no-files-found: error